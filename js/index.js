class taskControler {
  constructor(model, view) {
    this.model = model;
    this.view = view;
    // document.getElementById("task").addEventListener("click", (event) => {
    //   const task = document.getElementById("task").innerHTML;
    //   // console.log(event);
    //   // const newTask = ;
    //   // model.storeTask();
    // });
  }
}

const controler = new taskControler(null, null);
